kind: pipeline
name: build

environment: 
  REG_VARIABLE: myvar
 
steps:
- name: test
  image: ubuntu:18.04
  environment:
    SECRET_VARIABLE:
    from_secret: mysecretvar
  commands:
  - echo $REG_VARIABLE
  - cat README.md
  - envsubst < README.md | tee README.md
  - cat README.md
