kind: pipeline
name: build

environment: 
  REG_VARIABLE: myvar
 
steps:
- name: test
  image: ubuntu:18.04
  environment:
    SECRET_VARIABLE:
    from_secret: mysecretvar
    RETRIES: 0
  commands:
  - until [[ $RETRIES > 3 ]]; do RETRIES=$((RETRIES+1)); echo $RETRIES; sleep 5; done
  - echo $RETRIES
